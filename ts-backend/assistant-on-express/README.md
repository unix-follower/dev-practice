### Install dependencies
```shell
npm i
```
### Start server in dev mode
```shell
npm run start
```
### Define variables
```shell
export SERVER_URL=localhost:3000
```
To verify it is running
```shell
curl -v $SERVER_URL
```